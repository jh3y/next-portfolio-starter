import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import ThemeToggle from './'

<Meta title="Components/Theme Toggle" />

# Theme Toggle

This component switches the current theme in the app.
It's main purpose is toggling the `dark` class on `document.body` for `tailwindcss`.

<Canvas>
  <Story name="Default"><ThemeToggle/></Story>
</Canvas>